Installation
=============================================================
To install fest, just use ``pip`` like below:

.. code-block:: bash

    pip install feste

And that is it, ``pip`` will install all required dependencies.